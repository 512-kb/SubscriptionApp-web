// eslint-disable-next-line
const webURL = "https://kb512-web.herokuapp.com";
// eslint-disable-next-line
const apiURL = "https://kb512-api.herokuapp.com";

const localURL =
  window.location.protocol + "//" + window.location.hostname + ":" + 4000;

export default localURL;
